
const Main = imports.ui.main;

function init() {}

function enable() {
	Main.panel.statusArea['keyboard'].actor.hide();
}

function disable() {
	let indicator = Main.panel.statusArea['keyborad']

	if (indicator != null) {
		indicator.actor.show();
	}
}
